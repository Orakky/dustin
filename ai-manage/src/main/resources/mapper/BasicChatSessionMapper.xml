<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dustin.ai.manage.session.mapper.BasicChatSessionMapper">
    <select id="getBasicChatSessionBySessionId"
            resultType="com.dustin.ai.manage.session.entity.BasicChatSession">
        select bcs_id as bcsId,
               session_id as sessionId,
               session_name as sessionName,
               username,
               created_by as createdBy,
               created_time as createdTime,
               updated_by as updatedBy,
               updated_time as updatedTime,
               deleted_flg as deletedFlg,
               deleted_by as deletedBy,
               deleted_time as deletedTime
        from basic_chat_session
        where deleted_flg = '0'
        and session_id = '${sessionId}'
    </select>
</mapper>